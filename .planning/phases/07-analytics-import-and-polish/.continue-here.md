---
phase: 07-analytics-import-and-polish
task: 3 (checkpoint:human-verify)
total_tasks: 3 (in 07-05-PLAN.md)
status: post-checkpoint — bug fixes and feature work during user testing
last_updated: 2026-03-01
---

<current_state>
Phase 7 (final phase) is at checkpoint. All 5 plans (07-01 through 07-05) are complete.
The user has been doing live testing and we've been fixing bugs as they surface.
This session was entirely post-checkpoint bug fixing and one feature swap (Resend -> ZeptoMail).

All 6 commits from this session are on main.
</current_state>

<completed_work>
Previous sessions — see git log for full history. Key context:
- 07-05 Tasks 1-2 done (performance + mobile polish)
- 8 bug fixes during initial user testing (auth, invitations, settings, onboarding)

This session (2026-03-01) — 6 more commits:

1. **Soft-deactivate users on revoke** (`f0dcf60`)
   - Added `isActive Boolean @default(true)` to User model
   - Changed `revokeAcceptedUser()` from hard-delete to `isActive: false` + session sign-out
   - Added `PATCH /api/users/:id/reactivate` endpoint (admin-only)
   - Added Status column + Reactivate button to Team Members table
   - Updated confirmation dialog text for deactivation language

2. **React hooks ordering fix** (`6903edc`)
   - Moved `useState` for `reactivatingId` above early returns in team settings page

3. **Agent names not saving during invite acceptance** (`d2ee335`)
   - Root cause: `handle_new_user` trigger creates user with empty names (metadata has no name at invite time)
   - Fix: Separated API calls in accept-invite-form, added session refresh after password change
   - MEMORY.md updated: trigger is NOT dropped as previously noted

4. **Deactivated users could still log in** (`9ac97f9`)
   - `useUser` hook swallowed 401 from guard, kept metadata-derived profile
   - Fix: Detect "deactivated" error message, sign out, redirect to /login

5. **Replace Resend with ZeptoMail** (`de3c352`)
   - Removed `resend` npm package
   - Replaced with plain `fetch` to `api.zeptomail.com/v1.1/email`
   - Updated env vars: `ZEPTOMAIL_API_KEY`, `ZEPTOMAIL_FROM_ADDRESS`, `ZEPTOMAIL_FROM_NAME`
   - Only `notifications.service.ts` changed; schedulers/templates/preferences untouched

6. **Guard catch block swallowing deactivation exception** (`57b7fc9`)
   - `UnauthorizedException` was caught by the surrounding DB lookup try/catch
   - Fix: Re-throw `HttpException` instances from catch block
   - Switched to `ForbiddenException` (403) so api.ts won't trigger 401 retry logic
</completed_work>

<remaining_work>
- Task 3: checkpoint:human-verify (Phase 7 completion gate) still technically pending
- User has been actively testing and we've been fixing issues as they surface
- No known remaining blockers from testing

Outstanding items (from previous sessions):
- Phase 1 checkpoints (01-04, 01-05) still pending user verification
- `ZEPTOMAIL_API_KEY` needs to be set in .env for email sending to work
- Existing agent `wanderguard9100@gmail.com` has empty names — fixable via Settings > Profile
</remaining_work>

<decisions_made>
Previous session decisions (still valid):
- accept-mine endpoint: no @Roles decorator, placed before :id param routes
- Settings adminOnly: false — agents need Profile, Badge, Import tabs
- Guard Step 5 uses `as any` for role field
- Setup wizard: dynamic step list filtered by role

This session decisions:
- Soft-deactivate (isActive=false) instead of hard-delete on revoke — preserves all related data, allows reactivation
- Reactivation endpoint on users controller not invitations controller — simpler UX, team table already has user ID
- ForbiddenException (403) for deactivated users not 401 — prevents api.ts retry loop
- ZeptoMail via plain fetch not npm package — zero new dependencies
- Re-throw HttpException from guard DB lookup catch block — prevents swallowing intentional exceptions
- handle_new_user trigger is still active in DB — corrected MEMORY.md
</decisions_made>

<blockers>
None — all bugs from this session fixed and committed.
</blockers>

<context>
The project is at the end of Phase 7 (final phase). All planned features are built.
The user is doing live testing in a bug-fix/polish loop.

Key architectural knowledge from this session:
- handle_new_user Supabase trigger fires on auth.users INSERT (from inviteUserByEmail)
- At invite time, metadata only has invitation_id/tenant_id/role — NOT first_name/last_name
- Guard's DB lookup try/catch was hiding HttpExceptions — any future exception inside needs re-throw pattern
- api.ts only retries on 401, not 403 — use 403 for permanent blocks, 401 for token issues
- Revoke now soft-deactivates (isActive=false) instead of deleting — critical change from previous session's hard-delete approach

Config: quality profile, verifier=true, commit_docs=true, branching=none
</context>

<next_action>
Resume with `/gsd:resume-work`. Continue user testing loop.

To continue testing:
1. Run both servers: `pnpm --filter api dev` and `pnpm --filter web dev`
2. Test the 6 fixes from this session (especially soft-deactivate + ZeptoMail)
3. When ready, either approve the checkpoint or describe remaining issues

To approve and close out Phase 7:
- Run `/gsd:execute-phase 7` — it will detect 07-05 at checkpoint and re-present it
- Type "approved" to complete the phase
</next_action>
