---
phase: 09-founder-super-admin-dashboard
plan: 08
type: execute
wave: 5
depends_on: ["09-03", "09-05", "09-06", "09-07"]
files_modified:
  - apps/api/src/admin/admin.controller.ts
  - apps/api/src/admin/admin.service.ts
  - apps/admin/src/app/(admin)/audit-log/page.tsx
  - apps/admin/src/components/audit/audit-log-table.tsx
  - apps/admin/src/components/audit/audit-log-filters.tsx
  - apps/admin/src/app/(admin)/settings/page.tsx
  - apps/admin/src/components/settings/super-admin-list.tsx
  - apps/admin/src/components/settings/invite-super-admin-dialog.tsx
autonomous: false

must_haves:
  truths:
    - "Audit log page shows every super-admin action with timestamp, action type, target"
    - "Audit log is filterable by action type, target type, admin, and date range"
    - "Settings page shows list of super-admins with ability to add/remove"
    - "Admin app builds successfully"
    - "API builds successfully with all admin endpoints"
  artifacts:
    - path: "apps/admin/src/app/(admin)/audit-log/page.tsx"
      provides: "Audit log viewer page"
      min_lines: 30
    - path: "apps/admin/src/app/(admin)/settings/page.tsx"
      provides: "Super-admin management page"
      min_lines: 30
  key_links:
    - from: "apps/admin/src/app/(admin)/audit-log/page.tsx"
      to: "/admin/audit-logs"
      via: "api.get"
      pattern: "api.get.*admin/audit-logs"
    - from: "apps/admin/src/app/(admin)/settings/page.tsx"
      to: "/admin/super-admins"
      via: "api.get"
      pattern: "api.get.*admin/super-admins"
---

<objective>
Build the audit log viewer page, super-admin settings/management page, backend endpoints for super-admin CRUD, and verify full build of admin app + API.

Purpose: Complete the admin panel with audit trail visibility, super-admin management, and ensure everything builds.
Output: Working /audit-log and /settings pages. Full build verification.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-founder-super-admin-dashboard/09-CONTEXT.md
@.planning/phases/09-founder-super-admin-dashboard/09-RESEARCH.md
@.planning/phases/09-founder-super-admin-dashboard/09-03-SUMMARY.md
@.planning/phases/09-founder-super-admin-dashboard/09-05-SUMMARY.md
@.planning/phases/09-founder-super-admin-dashboard/09-06-SUMMARY.md
@.planning/phases/09-founder-super-admin-dashboard/09-07-SUMMARY.md
@apps/admin/src/lib/api.ts
@apps/admin/src/components/ui/data-table.tsx
@apps/admin/src/components/ui/confirmation-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Super-Admin CRUD Endpoints</name>
  <files>
    apps/api/src/admin/admin.controller.ts
    apps/api/src/admin/admin.service.ts
  </files>
  <action>
1. Add to **AdminService**:
   - `getSuperAdmins()`: Return all super-admins ordered by createdAt
   - `inviteSuperAdmin(email, firstName, lastName, invitedById)`:
     a. Call supabaseAdmin.auth.admin.inviteUserByEmail(email)
     b. Create SuperAdmin record with the auth user's ID
     c. Log via AuditService (action: superadmin.invite)
   - `removeSuperAdmin(superAdminId, removedById)`:
     a. Set isActive=false on SuperAdmin record (soft delete, NOT hard delete)
     b. Log via AuditService (action: superadmin.remove)
     c. Do NOT delete auth.users record — just deactivate from super_admins

2. Add to **AdminController**:
   - GET /admin/super-admins — list super-admins
   - POST /admin/super-admins/invite — invite new super-admin (body: { email, firstName, lastName })
   - DELETE /admin/super-admins/:id — remove super-admin (soft delete)
  </action>
  <verify>
`cd apps/api && pnpm exec tsc --noEmit` passes.
  </verify>
  <done>Super-admin CRUD endpoints functional: list, invite, and remove (soft delete) with audit logging.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Log Page and Super-Admin Settings Frontend</name>
  <files>
    apps/admin/src/app/(admin)/audit-log/page.tsx
    apps/admin/src/components/audit/audit-log-table.tsx
    apps/admin/src/components/audit/audit-log-filters.tsx
    apps/admin/src/app/(admin)/settings/page.tsx
    apps/admin/src/components/settings/super-admin-list.tsx
    apps/admin/src/components/settings/invite-super-admin-dialog.tsx
  </files>
  <action>
1. **AuditLogFilters** (audit-log-filters.tsx):
   - Filter dropdowns: Action type (from ADMIN_ACTIONS constant), Target type (user/tenant/super_admin), Super-admin (dropdown of admins)
   - Date range picker (start/end date inputs)
   - Reset filters button

2. **AuditLogTable** (audit-log-table.tsx):
   - Columns: Timestamp (formatted), Admin (firstName lastName), Action (human-readable label), Target Type, Target ID (truncated UUID), IP Address
   - Uses DataTable component
   - Action column uses a mapping from ADMIN_ACTIONS constant to human-readable labels (e.g., 'agency.suspend' -> 'Suspended Agency')
   - Expandable row detail showing full metadata JSON (if present)

3. **Audit Log page** (/audit-log/page.tsx): 'use client' component.
   - Page title "Audit Log"
   - AuditLogFilters at top
   - AuditLogTable below with server-side pagination
   - Fetches GET /admin/audit-logs with filter query params

4. **SuperAdminList** (super-admin-list.tsx):
   - Table showing super-admins: Name, Email, Status (active/inactive), Created
   - Remove button per row (except self — cannot remove yourself)
   - Remove uses ConfirmationDialog with destructive variant

5. **InviteSuperAdminDialog** (invite-super-admin-dialog.tsx):
   - Form: email, firstName, lastName
   - Validation via Zod (inviteSuperAdminSchema)
   - Calls POST /admin/super-admins/invite
   - Toast on success

6. **Settings page** (/settings/page.tsx):
   - Page title "Settings"
   - Section: "Super-Admin Team" with "Invite Super-Admin" button
   - SuperAdminList component
   - Fetches GET /admin/super-admins
  </action>
  <verify>
`pnpm --filter admin build` succeeds.
  </verify>
  <done>Audit log page shows filterable, paginated admin action history. Settings page shows super-admin list with invite and remove capabilities.</done>
</task>

<task type="auto">
  <name>Task 3: Full Build Verification</name>
  <files>
    (no specific files — this task fixes build errors across any file in the monorepo)
  </files>
  <action>
Run full build verification across the monorepo:

1. `pnpm --filter database exec prisma generate` — regenerate Prisma client
2. `pnpm --filter shared build` (or tsc --noEmit) — verify shared types compile
3. `pnpm --filter api build` — verify NestJS API builds with admin module
4. `pnpm --filter admin build` — verify admin Next.js app builds
5. `pnpm --filter web build` — verify tenant app still builds (regression check — JwtAuthGuard was modified)

Fix any build errors encountered. Common issues:
- Missing imports in admin.module.ts
- Type mismatches between shared types and API responses
- Missing workspace dependency resolution
- Prisma client not regenerated after schema changes
- transpilePackages missing for @anchor/shared in admin next.config.ts

If JwtAuthGuard changes caused issues in the web app build, fix them while maintaining the suspension check behavior.
  </action>
  <verify>
All 5 build commands pass: `pnpm --filter database exec prisma generate && pnpm --filter shared build && pnpm --filter api build && pnpm --filter admin build && pnpm --filter web build`
  </verify>
  <done>All packages build successfully: database, shared, api, admin, web. No regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete super-admin dashboard with:
  - Admin app at localhost:3002 with login, sidebar navigation
  - Dashboard with platform metrics, health alerts, growth charts
  - Agency management (list, detail with 4 tabs, suspend/unsuspend, export, limits)
  - User management (cross-tenant table, disable/enable, deactivate, role change, password reset)
  - Impersonation flow with banner and auto-expiry
  - Audit log viewer with filters
  - Super-admin settings with invite/remove
  - Tenant suspension enforcement in JwtAuthGuard
  </what-built>
  <how-to-verify>
  To test, you need to seed a super-admin record. Run this SQL in Supabase SQL Editor (replace with your auth user ID):
  ```sql
  INSERT INTO super_admins (id, email, first_name, last_name)
  VALUES ('<your-supabase-auth-user-id>', '<your-email>', 'Admin', 'User');
  ```

  Then:
  1. Start API: `cd apps/api && pnpm dev`
  2. Start admin app: `cd apps/admin && pnpm dev`
  3. Open http://localhost:3002 — should redirect to /login
  4. Log in with your seeded super-admin credentials
  5. Verify dashboard shows metric cards and charts
  6. Navigate to /agencies — verify agency list loads
  7. Click an agency — verify detail page with 4 tabs
  8. Navigate to /users — verify cross-tenant user list
  9. Navigate to /audit-log — verify audit entries appear
  10. Navigate to /settings — verify super-admin list
  11. Test suspend/unsuspend on an agency (use test agency if available)
  12. Verify tenant app (localhost:3000) blocks suspended agency users
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All monorepo packages build successfully
- Admin app accessible at localhost:3002
- Audit log page shows paginated admin actions
- Settings page shows super-admin management
- No build regressions in tenant web app
</verification>

<success_criteria>
- Audit log page: filterable by action, target, admin, date range (per locked decision)
- Settings page: list super-admins, invite new, remove existing (per locked decision)
- Full monorepo builds without errors
- Human verification of complete admin panel functionality
</success_criteria>

<output>
After completion, create `.planning/phases/09-founder-super-admin-dashboard/09-08-SUMMARY.md`
</output>
