---
phase: 09-founder-super-admin-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/package.json
  - apps/admin/src/app/layout.tsx
  - apps/admin/src/app/(auth)/login/page.tsx
  - apps/admin/src/app/(admin)/layout.tsx
  - apps/admin/src/app/(admin)/page.tsx
  - apps/admin/src/lib/api.ts
  - apps/admin/src/lib/supabase/client.ts
  - apps/admin/src/lib/supabase/server.ts
  - apps/admin/src/lib/supabase/middleware.ts
  - apps/admin/src/middleware.ts
  - apps/admin/src/components/layout/admin-sidebar.tsx
  - apps/admin/src/components/layout/admin-header.tsx
  - apps/admin/src/components/layout/impersonation-banner.tsx
  - apps/admin/next.config.ts
  - apps/admin/tsconfig.json
  - apps/admin/.env.local
autonomous: true

must_haves:
  truths:
    - "Admin app runs on port 3002 and shows login page at /login"
    - "Authenticated super-admin sees admin layout with sidebar navigation"
    - "Admin app resolves @anchor/shared types correctly"
    - "Unauthenticated users are redirected to /login"
  artifacts:
    - path: "apps/admin/src/app/(auth)/login/page.tsx"
      provides: "Super-admin login page"
      min_lines: 30
    - path: "apps/admin/src/app/(admin)/layout.tsx"
      provides: "Admin shell with sidebar"
      min_lines: 20
    - path: "apps/admin/src/lib/api.ts"
      provides: "Admin API client"
      min_lines: 30
    - path: "apps/admin/src/middleware.ts"
      provides: "Auth middleware"
      min_lines: 20
  key_links:
    - from: "apps/admin/src/middleware.ts"
      to: "Supabase auth"
      via: "getUser() check"
      pattern: "supabase.auth.getUser"
    - from: "apps/admin/src/lib/api.ts"
      to: "apps/api (NestJS)"
      via: "fetch to localhost:3001"
      pattern: "NEXT_PUBLIC_API_URL"
---

<objective>
Scaffold the admin Next.js app (apps/admin) in the monorepo with login page, admin layout with sidebar, API client, Supabase auth middleware, and placeholder dashboard page.

Purpose: Standalone admin app shell that all admin UI plans build upon.
Output: Working Next.js app at port 3002 with auth flow and admin layout.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-founder-super-admin-dashboard/09-CONTEXT.md
@.planning/phases/09-founder-super-admin-dashboard/09-RESEARCH.md
@apps/web/src/middleware.ts
@apps/web/src/lib/api.ts
@apps/web/src/lib/supabase/client.ts
@apps/web/src/lib/supabase/server.ts
@apps/web/src/lib/supabase/middleware.ts
@apps/web/next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Admin Next.js App</name>
  <files>
    apps/admin/package.json
    apps/admin/next.config.ts
    apps/admin/tsconfig.json
    apps/admin/src/app/layout.tsx
    apps/admin/src/app/globals.css
    apps/admin/.env.local
  </files>
  <action>
1. **Create the admin app** using `pnpm create next-app apps/admin --typescript --tailwind --eslint --app --src-dir --no-import-alias`. If the CLI prompts interactively, create the directory structure manually instead (follow apps/web structure).

2. **Update next.config.ts** to:
   - Set port via `env` or document that `--port 3002` is used in dev script
   - Add `transpilePackages: ['@anchor/shared']`
   - Match the existing apps/web next.config.ts structure but simpler (no rewrites needed)

3. **Update package.json**:
   - Set name to "admin"
   - Add dev script: `"dev": "next dev --port 3002"`
   - Add workspace dependency: `"@anchor/shared": "workspace:*"`
   - Install dependencies: `@supabase/ssr`, `@supabase/supabase-js`, `lucide-react`, `sonner`, `date-fns`, `zod`, `clsx`, `tailwind-merge`
   - Install dev dependencies matching apps/web pattern

4. **Create .env.local**:
```
NEXT_PUBLIC_SUPABASE_URL=<placeholder - same as web>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<placeholder - same as web>
NEXT_PUBLIC_API_URL=http://localhost:3001
```

5. **Create root layout.tsx** with dark/corporate admin theme — use Tailwind v4 CSS-based config (NOT tailwind.config.js). Add Inter font, sonner Toaster. Use a slate/dark color scheme to visually distinguish from the tenant app.

6. **Create globals.css** following Tailwind v4 pattern (CSS @theme). Use a darker accent color (slate-900 primary, blue-600 accent) to distinguish from tenant app.

Run `pnpm install` from the monorepo root after setup.
  </action>
  <verify>
`cd apps/admin && pnpm dev --port 3002` starts without errors (can terminate after confirming startup).
  </verify>
  <done>Admin app scaffolded in monorepo, runs on port 3002, resolves workspace dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Auth, Middleware, Layout, and API Client</name>
  <files>
    apps/admin/src/lib/supabase/client.ts
    apps/admin/src/lib/supabase/server.ts
    apps/admin/src/lib/supabase/middleware.ts
    apps/admin/src/middleware.ts
    apps/admin/src/lib/api.ts
    apps/admin/src/app/(auth)/login/page.tsx
    apps/admin/src/app/(admin)/layout.tsx
    apps/admin/src/app/(admin)/page.tsx
    apps/admin/src/components/layout/admin-sidebar.tsx
    apps/admin/src/components/layout/admin-header.tsx
    apps/admin/src/components/layout/impersonation-banner.tsx
  </files>
  <action>
1. **Supabase clients** (client.ts, server.ts, middleware.ts): Copy pattern from apps/web/src/lib/supabase/ but adapted for admin app. Same createBrowserClient/createServerClient patterns.

2. **middleware.ts**: Protect all routes except /login. Redirect unauthenticated to /login, authenticated away from /login. Follow the pattern from RESEARCH.md code example.

3. **api.ts**: Admin API client following apps/web/src/lib/api.ts pattern exactly:
   - Base URL from NEXT_PUBLIC_API_URL
   - Auto-attach Bearer token from Supabase session
   - Auto-retry on 401 with token refresh
   - GET, POST, PUT, PATCH, DELETE methods
   - All requests go to `/admin/*` prefix endpoints

4. **Login page** (/login): Simple email+password form styled with corporate/dark theme. Uses Supabase signInWithPassword. On success, redirect to /. Include "Super Admin Portal" branding. Form fields: email, password. Error display for invalid credentials. NO signup link (super-admins are invited/seeded).

5. **Admin layout** ((admin)/layout.tsx): Sidebar + header + main content area.
   - Sidebar with navigation: Dashboard (/), Agencies (/agencies), Users (/users), Audit Log (/audit-log), Settings (/settings)
   - Use lucide-react icons: LayoutDashboard, Building2, Users, ScrollText, Settings
   - Header with super-admin name/email, logout button
   - ImpersonationBanner component: amber/orange banner at the top when impersonating, shows "Acting as [Agency Name]" with "End Session" button. Initially hidden (state driven by parent or context later).

6. **Dashboard placeholder** ((admin)/page.tsx): Simple page with "Platform Dashboard" heading and "Loading metrics..." placeholder text. This will be populated in Plan 05.

Style all components with Tailwind using the dark/corporate admin theme (slate-900 sidebar, slate-800 cards, blue-600 accent).
  </action>
  <verify>
`cd apps/admin && pnpm build` succeeds. Login page renders at localhost:3002/login. Authenticated users see admin layout with sidebar.
  </verify>
  <done>Admin app has working auth flow, protected routes, admin layout with sidebar navigation, and API client ready for backend integration.</done>
</task>

</tasks>

<verification>
- `pnpm --filter admin build` succeeds
- Admin app accessible at localhost:3002
- /login page renders with email+password form
- Unauthenticated users redirected to /login
- Admin sidebar shows 5 navigation items
- API client correctly attaches auth tokens
</verification>

<success_criteria>
- Admin Next.js app exists in apps/admin, runs on port 3002
- Login page functional with Supabase auth
- Admin layout with sidebar navigation renders for authenticated users
- Middleware properly protects routes
- API client ready for /admin/* endpoints
- Impersonation banner component exists (hidden by default)
</success_criteria>

<output>
After completion, create `.planning/phases/09-founder-super-admin-dashboard/09-02-SUMMARY.md`
</output>
