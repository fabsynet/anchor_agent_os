---
phase: 08-scheduled-emails-and-client-communications
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - apps/web/src/app/(dashboard)/settings/communications/page.tsx
  - apps/web/src/components/settings/settings-nav.tsx
  - apps/web/src/app/(dashboard)/communications/page.tsx
  - apps/web/src/app/(dashboard)/communications/compose/page.tsx
  - packages/shared/src/constants/roles.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Admin can toggle birthday and renewal reminder email settings on/off"
    - "Admin can compose and send a bulk email from the UI"
    - "Email send history is visible with type filters and pagination"
    - "Communications nav item appears in the sidebar"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/settings/communications/page.tsx"
      provides: "Email settings toggle page"
      min_lines: 40
    - path: "apps/web/src/app/(dashboard)/communications/page.tsx"
      provides: "Email history list page"
      min_lines: 50
    - path: "apps/web/src/app/(dashboard)/communications/compose/page.tsx"
      provides: "Bulk email compose page"
      min_lines: 50
    - path: "packages/shared/src/constants/roles.ts"
      provides: "Communications nav item in NAV_ITEMS"
      contains: "communications"
  key_links:
    - from: "apps/web/src/app/(dashboard)/communications/page.tsx"
      to: "/api/communications/history"
      via: "api.get fetch"
      pattern: "api.*communications/history"
    - from: "apps/web/src/app/(dashboard)/communications/compose/page.tsx"
      to: "/api/communications/send"
      via: "api.post fetch"
      pattern: "api.*communications/send"
    - from: "apps/web/src/app/(dashboard)/settings/communications/page.tsx"
      to: "/api/communications/settings"
      via: "api.get and api.patch"
      pattern: "api.*communications/settings"
---

<objective>
Build the frontend pages for email communications: settings page for email toggles, email history page, and bulk email composer.

Purpose: Delivers the user-facing UI for all Phase 8 features. Admin can configure email settings, compose bulk emails, and everyone can view email send history.
Output: Three new pages plus navigation updates.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scheduled-emails-and-client-communications/08-RESEARCH.md
@.planning/phases/08-scheduled-emails-and-client-communications/08-01-SUMMARY.md
@.planning/phases/08-scheduled-emails-and-client-communications/08-02-SUMMARY.md
@.planning/phases/08-scheduled-emails-and-client-communications/08-03-SUMMARY.md
@apps/web/src/components/settings/settings-nav.tsx
@packages/shared/src/constants/roles.ts
@apps/web/src/app/(dashboard)/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation Updates + Email Settings Page</name>
  <files>
    packages/shared/src/constants/roles.ts
    apps/web/src/components/settings/settings-nav.tsx
    apps/web/src/app/(dashboard)/settings/communications/page.tsx
  </files>
  <action>
    **1. Add Communications nav item** to `packages/shared/src/constants/roles.ts`:
    - Add before the 'settings' entry in NAV_ITEMS:
      ```typescript
      { id: 'communications', label: 'Communications', href: '/communications', icon: 'Mail', adminOnly: false },
      ```
    - This goes BEFORE settings (last item). Communications is visible to all roles since the history page is useful for everyone.

    **2. Add Communications tab to SettingsNav** (`apps/web/src/components/settings/settings-nav.tsx`):
    - Add to SETTINGS_TABS array:
      ```typescript
      { label: 'Communications', href: '/settings/communications', adminOnly: true },
      ```
    - Place after 'Badge' and before 'Import' in the array.

    **3. Create Email Settings Page** (`apps/web/src/app/(dashboard)/settings/communications/page.tsx`):
    - 'use client' directive
    - Admin-only page. If `!isAdmin`, redirect to /settings/profile (same pattern as team page).
    - Fetch current settings from `GET /api/communications/settings` on mount.
    - Display the SettingsNav at top (import from '@/components/settings/settings-nav').
    - Page title: "Communications Settings"
    - Description: "Configure automated email notifications sent to your clients."

    **Sections with toggle switches:**

    a) **Birthday Emails** section:
       - Toggle: "Send birthday greeting emails" with description "Automatically send a birthday email to clients with a birthday and email address on file."
       - Bound to `birthdayEmailsEnabled`.

    b) **Renewal Reminder Emails** section:
       - Title: "Renewal Reminder Emails"
       - Description: "Send email reminders to clients before their policy expires."
       - Three toggles:
         - "60 days before expiry" -> `renewalReminder60Days`
         - "30 days before expiry" -> `renewalReminder30Days`
         - "7 days before expiry" -> `renewalReminder7Days`

    **Implementation details:**
    - Use the inline toggle switch pattern established in Phase 5 (custom switch, not Radix). Check how Phase 5 expenses settings or user preferences implemented toggle switches and follow same pattern.
    - On toggle change, immediately PATCH to `PATCH /api/communications/settings` with the changed field.
    - Show loading state during save. Show success toast via sonner on save.
    - Handle API errors with error toast.
    - Use the existing `api` helper from `@/lib/api` for authenticated requests.

    Follow existing page patterns:
    - Page header with title and description
    - Card-based layout for each section
    - Tailwind styling consistent with other settings pages
  </action>
  <verify>
    Run `pnpm --filter web build` succeeds. Settings page renders without errors. Communications appears in sidebar nav and settings sub-nav.
  </verify>
  <done>
    Communications nav item appears in sidebar. Communications tab appears in settings sub-nav (admin only). Email settings page shows toggle switches for birthday and renewal reminder emails that persist to backend on change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Email History Page + Bulk Email Compose Page</name>
  <files>
    apps/web/src/app/(dashboard)/communications/page.tsx
    apps/web/src/app/(dashboard)/communications/compose/page.tsx
  </files>
  <action>
    **1. Create Email History Page** (`apps/web/src/app/(dashboard)/communications/page.tsx`):
    - 'use client' directive
    - Page title: "Communications"
    - Description: "View sent email history and compose new messages."

    **Layout:**
    - Top right: "Compose Email" button (only shown to admin users via `isAdmin` check from useUser). Links to `/communications/compose`.
    - Filter bar:
      - Type filter: Select dropdown with options from EMAIL_TYPES constant (import from @anchor/shared), plus "All Types" option using _none sentinel pattern.
      - Optional: Client search filter (text input or omit for MVP simplicity -- can just use type filter).
    - Email history table with columns:
      - Date/Time: format sentAt with date-fns `format(new Date(log.sentAt), 'MMM d, yyyy h:mm a')`
      - Type: Display label from EMAIL_TYPES constant (use badge/chip styling)
      - Recipient: recipientEmail (and client name if available: `${log.client?.firstName} ${log.client?.lastName}`)
      - Subject: log.subject (truncate if long)
      - Status: Badge with color -- 'sent' = green, 'failed' = red, 'queued' = yellow
      - Sent By: log.sentBy name or "System" if null (automated emails have no sentBy)
    - Pagination at bottom (follow existing pagination pattern from clients/policies pages).

    **Data fetching:**
    - Use `api.get<{ data: EmailLog[]; total: number; page: number; limit: number }>(\`/api/communications/history?page=${page}&limit=20${type ? \`&type=${type}\` : ''}\`)`
    - Import EmailLog type from @anchor/shared.
    - Refresh on filter change and page change.

    **Empty state:** "No emails have been sent yet." with a brief explanation.

    **2. Create Bulk Email Compose Page** (`apps/web/src/app/(dashboard)/communications/compose/page.tsx`):
    - 'use client' directive
    - Admin-only page. If `!isAdmin`, redirect to /communications.
    - Page title: "Compose Email"
    - Back link to /communications.

    **Form layout (Card-based):**
    - **Recipient Filter:** Select dropdown with options from RECIPIENT_FILTERS constant.
      - "All Clients & Leads" (default)
      - "Clients Only"
      - "Leads Only"

    - **Subject:** Text input, max 200 chars.

    - **Message Body:** Textarea (large, ~8 rows), max 10000 chars. Plain text for MVP.
      - Helper text below: "This message will be sent as a service communication from your agency."

    - **Send Button:** "Send Email" with Mail icon. Confirm before sending via a confirm dialog or window.confirm() for MVP simplicity.

    **On submit:**
    - Validate: subject and body required.
    - Show confirmation: "Are you sure you want to send this email to all [filter description] with email addresses on file?"
    - POST to `/api/communications/send` with `{ subject, body, recipientFilter }`.
    - On success: Show sonner toast "Email sent to X recipients", redirect to /communications.
    - On error: Show error toast with message.
    - Disable button during send (loading state).

    **Important UI note:** Include a subtle warning banner at the top:
    "Bulk emails are for operational announcements only (e.g., office hours changes, important notices). Do not use for marketing or promotional content."

    Follow existing page patterns:
    - Use Card component for form sections
    - Use Button, Input, Textarea, Label, Select from existing UI components
    - Tailwind styling consistent with other forms
    - Use sonner for toast notifications
  </action>
  <verify>
    Run `pnpm --filter web build` succeeds. Both pages compile without errors.
    Navigate to /communications -- history page loads.
    Navigate to /communications/compose (as admin) -- compose form loads.
  </verify>
  <done>
    Email history page shows paginated, filterable list of all sent emails with type, recipient, subject, status, and sender.
    Bulk email compose page lets admin select recipients, write subject/body, and send with confirmation.
    Admin-only compose button and page properly gated.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter web build` succeeds
- `pnpm --filter shared build` succeeds (after NAV_ITEMS update)
- Communications appears in sidebar navigation
- Settings > Communications tab shows for admin only
- Email toggles save to backend on change
- Email history page loads with type filter and pagination
- Compose page validates input and sends with confirmation
- Non-admin users see history but not compose button
</verification>

<success_criteria>
- NAV_ITEMS includes Communications with Mail icon
- SettingsNav includes Communications tab (admin only)
- Settings/Communications page: 4 toggle switches that persist to backend
- Communications page: Email history table with type filter, pagination, status badges
- Communications/Compose page: Recipient filter, subject, body form with send confirmation
- Admin gating on compose page and compose button
- API calls use correct endpoints from Plan 03
- All pages follow existing codebase styling patterns
</success_criteria>

<output>
After completion, create `.planning/phases/08-scheduled-emails-and-client-communications/08-04-SUMMARY.md`
</output>
