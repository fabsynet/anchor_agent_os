---
phase: 08-scheduled-emails-and-client-communications
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All Phase 8 features work end-to-end"
    - "Email settings toggles persist correctly"
    - "Email history shows sent emails"
    - "Bulk email compose and send works"
    - "Cron jobs are registered"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for all Phase 8 features.

Purpose: Confirm that all five success criteria are met before marking Phase 8 complete.
Output: User approval or list of issues to address.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-scheduled-emails-and-client-communications/08-01-SUMMARY.md
@.planning/phases/08-scheduled-emails-and-client-communications/08-02-SUMMARY.md
@.planning/phases/08-scheduled-emails-and-client-communications/08-03-SUMMARY.md
@.planning/phases/08-scheduled-emails-and-client-communications/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Verification</name>
  <files></files>
  <action>
    Run full build to ensure everything compiles:
    1. `pnpm --filter database exec prisma generate`
    2. `pnpm --filter @anchor/shared build` (or `pnpm --filter shared build`)
    3. `pnpm --filter api build`
    4. `pnpm --filter web build`

    If any build fails, investigate and fix the issue before proceeding to checkpoint.

    Also verify the API starts successfully:
    - Check that NestJS logs show all cron jobs registered (should see 5 total: renewals 1AM, expenses 2AM, renewal reminders 7AM, birthday 7:30AM, digest 8AM)
    - Check that CommunicationsModule is registered
  </action>
  <verify>
    All 4 build commands succeed with exit code 0.
  </verify>
  <done>
    Full monorepo builds cleanly. All modules registered. All cron jobs registered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Quick Smoke Test</name>
  <files></files>
  <action>
    With the API and web app running locally:

    1. Test email settings endpoint:
       - `GET /api/communications/settings` should return default settings (all true) or the stored settings
       - `PATCH /api/communications/settings` with `{ "birthdayEmailsEnabled": false }` should succeed

    2. Test email history endpoint:
       - `GET /api/communications/history` should return `{ data: [], total: 0, page: 1, limit: 20 }` (empty initially)

    3. Verify the settings page loads at `/settings/communications` (admin only).

    4. Verify the communications page loads at `/communications`.

    If any endpoint returns unexpected errors, fix the issue.
  </action>
  <verify>
    All endpoints return expected responses. Pages load without runtime errors.
  </verify>
  <done>
    API endpoints respond correctly. Frontend pages render without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 8: Scheduled Emails and Client Communications.

    Features built:
    1. **Birthday Email System**: Cron job at 7:30 AM Toronto time sends birthday greeting emails to clients with a birthday and email on file. Idempotent (won't double-send).
    2. **Renewal Reminder Email System**: Cron job at 7:00 AM sends policy renewal reminder emails to clients at 60, 30, and 7 days before expiry. Each interval is independently toggleable by admin.
    3. **Email Settings Page**: Settings > Communications tab (admin only) with toggle switches for birthday emails and each renewal reminder interval.
    4. **Bulk Email Compose**: /communications/compose page (admin only) to send service announcements to all clients, clients only, or leads only.
    5. **Email History**: /communications page showing all sent emails with type filter, recipient info, status, and pagination.
    6. **Data Foundation**: EmailLog model for tracking, TenantEmailSettings model for configuration, generic sendEmail/sendBatchEmail methods.
  </what-built>
  <how-to-verify>
    Start both API (`pnpm --filter api dev`) and web (`pnpm --filter web dev`).

    **1. Settings Page (Admin):**
    - Navigate to Settings > Communications
    - Verify 4 toggle switches appear (birthday, 60-day, 30-day, 7-day)
    - Toggle one off, refresh page, confirm it stays off
    - Toggle it back on

    **2. Communications Page:**
    - Navigate to Communications in sidebar
    - Verify "Compose Email" button appears (admin only)
    - Verify type filter dropdown works
    - Page should show empty state initially

    **3. Compose Email (Admin):**
    - Click "Compose Email"
    - Select recipient filter
    - Enter subject and message body
    - Click send and confirm
    - Verify success toast (or expected error if no ZeptoMail key configured)
    - Navigate back to Communications -- verify email(s) appear in history

    **4. Cron Job Verification:**
    - Check API console logs on startup for registered cron jobs
    - Birthday and renewal reminder crons are time-based (7:00/7:30 AM) so they won't fire during testing
    - Verify they are registered by checking NestJS startup logs

    **5. Cross-check:**
    - Non-admin user should NOT see "Compose Email" button
    - Non-admin user should NOT see Communications tab in Settings
    - Non-admin user SHOULD see Communications in sidebar and email history
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 8 complete, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- Full monorepo builds
- All API endpoints respond correctly
- All frontend pages render
- Cron jobs registered on API startup
- Admin-only features properly gated
</verification>

<success_criteria>
- User confirms all 5 Phase 8 success criteria are met
- No critical bugs found during verification
</success_criteria>

<output>
After completion, create `.planning/phases/08-scheduled-emails-and-client-communications/08-05-SUMMARY.md`
</output>
